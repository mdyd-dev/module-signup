---
name: token_is_valid
---
{%- query_graph 'get_user_with_password_token', id: params.id, result: g -%}
{%- assign token_valid = params.token | is_token_valid: params.id -%}
{% assign user = g.people.results.first %}
{% if user.id != blank and token_valid == true and user.default.password_token == params.token %}true{% endif %}
